# Copy to .env (same folder) and fill in your own values.
# .env is gitignored - secrets must not end up in the repo or in the installer.

# Free key from https://www.weatherapi.com/ - used by the weather widget.
YASB_WEATHER_API_KEY=
