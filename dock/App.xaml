<Application x:Class="OmarchyDock.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OmarchyDock"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- The dock's right-click menu. Templated from scratch because the
             stock ContextMenu renders in the system light style and sat in the
             middle of a dark dock looking like a different program's window.
             Brushes are DynamicResource: ThemeLoader puts the Mocha palette
             into these same application resources at startup, after this file
             has been parsed. -->

        <Style TargetType="Separator" x:Key="{x:Static MenuItem.SeparatorStyleKey}">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" Margin="10,5"
                                Background="{DynamicResource Surface1Brush}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Row" CornerRadius="7" Padding="8,7" Margin="4,1"
                                Background="Transparent">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Always present, only revealed when checked, so a
                                     checkable item does not shift its text sideways
                                     as it toggles. -->
                                <Path x:Name="Tick" Grid.Column="0"
                                      Width="11" Height="9"
                                      Stretch="Uniform"
                                      Opacity="0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      Fill="{DynamicResource MauveBrush}"
                                      Data="M 0,4.5 L 3.6,8.4 L 10.5,0.6 L 9.2,0 L 3.6,6.4 L 1.1,3.8 Z" />

                                <ContentPresenter Grid.Column="1"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Tick" Property="Opacity" Value="1" />
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Row" Property="Background"
                                        Value="{DynamicResource Surface0Brush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Overlay0Brush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <!-- Without a drop shadow the popup is opaque, and the rounded
                 corners would be filled in by the system window behind them. -->
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border CornerRadius="11" Padding="4"
                                Background="{DynamicResource MantleBrush}"
                                BorderBrush="{DynamicResource Surface1Brush}"
                                BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="18" ShadowDepth="4"
                                                  Direction="270" Opacity="0.45" Color="Black" />
                            </Border.Effect>
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
